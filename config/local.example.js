// To set up the config values for local development you can make a copy of this file and
// rename it as local.js in this folder. These values will take priority over the default.js
// settings.

const config = {
  apiKey: 'YOURAPIKEYHERE',
};

module.exports = config;
